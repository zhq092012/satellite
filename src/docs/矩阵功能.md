question：
1.删除layer=1的空中无人机，将地面接收站的layer调整为layer=1
2.在 <button class="tab-btn" :class="{ active: currentView === 'AAR' }" @click="currentView = 'AAR'">
            🧮 战术算法矩阵
          </button> 点击事件中调用generateMatrices计算四种矩阵，并设计对应的显示组件


# 战术算法矩阵与资产分层调整实现计划

## 概述
本计划涵盖两个主要需求：
1. **资产分层与初始化调整**：删除原 `layer=1` 的 3 架空中中继无人机，将 5 个地面雷达/接收站的立体分层调整为 `layer=1`（空基/接收层）。
2. **战术算法矩阵触发与可视化组件**：在顶部导航“🧮 战术算法矩阵”按钮点击事件中，通过 Web Worker 触发解算 `generateMatrices`，并设计开发全新的 `TacticalMatrixView.vue` 可视化组件展示四大战术算力矩阵（空间过境矩阵、星地通视矩阵、传输时延矩阵、武器打击矩阵）。

---

## Proposed Changes

### 1. 数据库种子与资产数据调整 (`src/db/`)

#### [MODIFY] [seeder.ts](file:///Users/zhanghq/Documents/GitHub/ElectronicSimulation/src/db/seeder.ts)
- 从 `assets` 初始数据中移除 3 架中继无人机 (`relay-drone-01`, `relay-drone-02`, `relay-drone-03`)。
- 将 5 个接收站 (`station-hualien`, `station-hengchun`, `station-keelung`, `station-taitung`, `station-penghu`) 的 `layer` 属性由 `0` 修改为 `1`。
- 更新 `communication_windows` 静态骨干链路种子数据：移除原无人机-接收站静态链路，保留并更新接收站-指挥中心（Layer 1 -> Layer 0）骨干链路。

---

### 2. Web Worker 与 SQLite 客户端支持 (`src/workers/`, `src/db/`)

#### [MODIFY] [sqlite.worker.ts](file:///Users/zhanghq/Documents/GitHub/ElectronicSimulation/src/workers/sqlite.worker.ts)
- 在 `WorkerRequest` 类型中新增 `"GENERATE_MATRICES"` 消息类型。
- 在 `message` 监听器中捕获 `"GENERATE_MATRICES"` 指令，调用已实现的 `generateMatrices(scenarioId)` 函数，并将解算的 4 组矩阵数据 (`passMatrix`, `visibleMatrix`, `overheadMatrix`, `attackMatrix`) 通过 `postMessage` 返回给主线程。

#### [MODIFY] [sqlite-client.ts](file:///Users/zhanghq/Documents/GitHub/ElectronicSimulation/src/db/sqlite-client.ts)
- 更新 `send` 方法的 `type` 参数类型定义，允许 `"GENERATE_MATRICES"`。
- 新增 `generateMatrices(scenarioId: string = 'scen-001')` 异步公共方法，用于从 Web Worker 异步请求矩阵解算数据。

---

### 3. 前端主视图与矩阵展示组件 (`src/App.vue`, `src/components/electronic/`)

#### [MODIFY] [App.vue](file:///Users/zhanghq/Documents/GitHub/ElectronicSimulation/src/App.vue)
- 拓展 `currentView` 类型：`ref<"SANDBOX" | "AAR" | "MATRIX">("SANDBOX")`。
- 修改顶部导航中“🧮 战术算法矩阵”按钮的绑定：
  - `:class="{ active: currentView === 'MATRIX' }"`
  - `@click="switchToMatrixView()"`
- 实现 `switchToMatrixView()` 方法：将 `currentView` 设置为 `'MATRIX'`，并调用 `sqliteClient.generateMatrices()` 加载矩阵数据。
- 在主界面逻辑中条件渲染新组件 `<TacticalMatrixView v-else-if="currentView === 'MATRIX'" :matrices="matrixData" :loading="matrixLoading" @refresh="loadMatrices" />`。

#### [NEW] [TacticalMatrixView.vue](file:///Users/zhanghq/Documents/GitHub/ElectronicSimulation/src/components/electronic/TacticalMatrixView.vue)
- 采用深色暗黑科技感 UI（深蓝/青色/荧光绿暗黑微光与玻璃拟态）。
- **顶层统计指标卡 (KPI Cards)**：
  - 空间过境卫星总次
  - 活跃星地通视链路数
  - 平均传输与处理时延 (s)
  - 武器有效打击窗口数
- **矩阵分类切换 Tab 选项卡**：
  1. **空间过境矩阵 (Pass Matrix)**：显示卫星 ID、轨交战场区域时间窗口列表、过境时长。
  2. **星地通视矩阵 (Visibility Matrix)**：显示星地节点对 (Satellite ↔ Station)、通视开始/结束时间序列、通视持续时长。
  3. **传输时延矩阵 (Overhead Matrix)**：显示传输链路 (Layer 2→1 / Layer 1→0)、传输时延、星载/接收站处理开销、动态交战加成时延、链路当前状态 (TRANSMITTING / JAMMED / DESTROYED)、全时序 Tick 变化折线图。
  4. **武器打击矩阵 (Attack Matrix)**：显示红方武器名称、打击杀伤类型 (SOFT/HARD)、蓝方目标节点及 Layer、理论时延/实际已叠加时延、是否实际执行打击、有效打击时间窗口。
- 包含搜索过滤、手动刷新解算、可视化 Gantt/时间轴条带展示。

---

## Verification Plan

### Automated Tests
- 编译检查：运行 `npm run dev` 确保无 TypeScript 或 Vue 模板编译错误。

### Manual Verification
1. 启动应用并重置数据库，确认沙盘上的 3 架无人机已移除，5 个地基接收站已调整为 `layer=1`（空基/接收层）。
2. 点击顶部导航栏的 **🧮 战术算法矩阵** 按钮：
   - 确认视图正确切换到战术算法矩阵界面，且 tab 按钮被高亮显示。
   - 检查控制台及 UI，确认四大算力矩阵（过境、通视、时延、打击）解算数据正确返回并清晰呈现。
3. 切换子 Tab（过境/通视/时延/打击）并测试搜索过滤与刷新功能，验证数据渲染正确性与科技感视觉效果。
